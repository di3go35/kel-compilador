fn suma(nums: [int], n: int) -> int {
  var total = 0
  for i in 0..n {
    total = total + nums[i]
  }
  return total
}

fn main() {
  var xs: [int] = [10, 20, 30]
  xs[0] = 5
  println(suma(xs, 3))
  println(xs[2])
}
